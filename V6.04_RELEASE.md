# JejakNasab V6.04 — Public Link Compatibility Fix

- Public API accepts new short tokens and legacy tokens containing dots.
- Public API failures are returned as JSON diagnostics instead of generic HTML errors.
- Frontend displays HTTP status when an API response is not JSON.
- Health endpoint reports version 6.04.
- No database reset or destructive migration.
- Existing public tokens are preserved.
